// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Utils from "../../Utils.res.mjs";
import * as Array2D from "../../Array2D.res.mjs";
import * as Belt_Array from "rescript/lib/es6/Belt_Array.js";
import * as Stdlib__Int from "@dsiu/rescript-stdlib-fp/src/Stdlib__Int.res.mjs";
import * as Stdlib__Array from "@dsiu/rescript-stdlib-fp/src/Stdlib__Array.res.mjs";
import * as Stdlib__Option from "@dsiu/rescript-stdlib-fp/src/Stdlib__Option.res.mjs";
import * as Stdlib__Function from "@dsiu/rescript-stdlib-fp/src/Stdlib__Function.res.mjs";

function log(prim) {
  console.log(prim);
}

function log2(prim0, prim1) {
  console.log(prim0, prim1);
}

function parse(data) {
  return Stdlib__Array.reduceWithIndex(Utils.splitNewline(data).map(__x => Stdlib__Function.compose(prim => prim.trim(), Utils.splitChars, __x)), [], (acc, row, _r) => Belt_Array.concatMany([
    acc,
    [row.map((col, _c) => Stdlib__Option.getOr(Stdlib__Int.fromString(col, undefined), -1))]
  ]));
}

function solvePart1(data) {
  let grid = parse(data);
  let starts = Array2D.reduceWithIndex(grid, [], (acc, v, param) => {
    if (v === 0) {
      return Belt_Array.concatMany([
        acc,
        [[
            param[1],
            param[0]
          ]]
      ]);
    } else {
      return acc;
    }
  });
  let goals = Array2D.reduceWithIndex(grid, [], (acc, v, param) => {
    if (v === 9) {
      return Belt_Array.concatMany([
        acc,
        [[
            param[1],
            param[0]
          ]]
      ]);
    } else {
      return acc;
    }
  });
  let tMap = {
    grid: grid,
    starts: starts,
    goals: goals
  };
  console.log(tMap);
  return 1;
}

function solvePart2(data) {
  return 2;
}

export {
  log,
  log2,
  parse,
  solvePart1,
  solvePart2,
}
/* Utils Not a pure module */
