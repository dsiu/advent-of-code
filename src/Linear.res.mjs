// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_object from "rescript/lib/es6/Primitive_object.js";

function cmp(param, param$1) {
  let r = Primitive_object.compare(param[0], param$1[0]);
  if (r !== 0) {
    return r;
  } else {
    return Primitive_object.compare(param[1], param$1[1]);
  }
}

function map(param, f) {
  return [
    f(param[0]),
    f(param[1])
  ];
}

let V2 = {
  cmp: cmp,
  map: map
};

function cmp$1(param, param$1) {
  let r = Primitive_object.compare(param[0], param$1[0]);
  if (r !== 0) {
    return r;
  }
  let r$1 = Primitive_object.compare(param[1], param$1[1]);
  if (r$1 !== 0) {
    return r$1;
  } else {
    return Primitive_object.compare(param[2], param$1[2]);
  }
}

function map$1(param, f) {
  return [
    f(param[0]),
    f(param[1]),
    f(param[2])
  ];
}

let V3 = {
  cmp: cmp$1,
  map: map$1
};

export {
  V2,
  V3,
}
/* No side effect */
