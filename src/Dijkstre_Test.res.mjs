// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Dijkstra from "./Dijkstra.res.mjs";
import * as Graph_Ext from "./Graph_Ext.res.mjs";
import * as AdjacencyList from "./AdjacencyList.res.mjs";

function log(prim) {
  console.log(prim);
}

function log2(prim0, prim1) {
  console.log(prim0, prim1);
}

Graph_Ext.Make(AdjacencyList.Node.$$String);

let Dijkstra$1 = Dijkstra.Dijkstra(AdjacencyList.Node.$$String);

let g = AdjacencyList.Node.$$String.make();

AdjacencyList.Node.$$String.addDirectedEdge(g, "1", "2", 7);

AdjacencyList.Node.$$String.addDirectedEdge(g, "1", "3", 9);

AdjacencyList.Node.$$String.addDirectedEdge(g, "1", "6", 14);

AdjacencyList.Node.$$String.addDirectedEdge(g, "2", "3", 10);

AdjacencyList.Node.$$String.addDirectedEdge(g, "2", "4", 15);

AdjacencyList.Node.$$String.addDirectedEdge(g, "3", "4", 11);

AdjacencyList.Node.$$String.addDirectedEdge(g, "3", "6", 2);

AdjacencyList.Node.$$String.addDirectedEdge(g, "4", "5", 6);

AdjacencyList.Node.$$String.addDirectedEdge(g, "5", "6", 9);

let match = Dijkstra$1.dijkstra(g, "1");

Array.from(match[0].entries()).forEach(param => {
  let distance = param[1];
  let node = param[0];
  if (distance !== undefined) {
    console.log("Distance from 1 to", node, "is", distance);
  } else {
    console.log("Node", node, "is unreachable from 1");
  }
});

Array.from(match[1].entries()).forEach(param => {
  let pNode = param[1];
  let node = param[0];
  if (pNode !== undefined) {
    console.log(node, " has previous node ", pNode);
  } else {
    console.log("Node", node, "does not have a previous node");
  }
});

let shortestPaths = Dijkstra$1.shortestPaths(g, "1");

console.log(shortestPaths, "Shortest paths from 1");

export {
  log,
  log2,
}
/*  Not a pure module */
