// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Int from "rescript/lib/es6/belt_Int.js";
import * as Belt_Array from "rescript/lib/es6/belt_Array.js";
import * as Pervasives from "rescript/lib/es6/pervasives.js";
import * as Belt_Option from "rescript/lib/es6/belt_Option.js";
import * as Utils$AdventOfCode from "../../Utils.mjs";

function log(prim) {
  console.log(prim);
}

function makeSegment(str) {
  var direction = str[0];
  var steps = Belt_Option.getExn(Belt_Int.fromString(str.slice(1)));
  switch (direction) {
    case "D" :
        return {
                direction: /* South */1,
                steps: steps
              };
    case "L" :
        return {
                direction: /* West */2,
                steps: steps
              };
    case "R" :
        return {
                direction: /* East */0,
                steps: steps
              };
    case "U" :
        return {
                direction: /* North */3,
                steps: steps
              };
    default:
      return Pervasives.failwith("Invalid direction");
  }
}

function facing(direction) {
  switch (direction) {
    case /* East */0 :
        return [
                1,
                0
              ];
    case /* South */1 :
        return [
                0,
                -1
              ];
    case /* West */2 :
        return [
                -1,
                0
              ];
    case /* North */3 :
        return [
                0,
                1
              ];
    
  }
}

function parse(data) {
  return Belt_Array.map(Utils$AdventOfCode.splitNewline(data), (function (x) {
                return x.trim().split(",");
              }));
}

function solvePart1(data) {
  var segs = Belt_Array.map(parse(data), (function (__x) {
          return Belt_Array.map(__x, makeSegment);
        }));
  console.log(segs);
  return 1;
}

function solvePart2(data) {
  return 2;
}

var TC;

var $$Location;

export {
  log ,
  TC ,
  $$Location ,
  makeSegment ,
  facing ,
  parse ,
  solvePart1 ,
  solvePart2 ,
}
/* No side effect */
