// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_List from "@dsiu/rescript-stdlib-fp/src/Stdlib_List.mjs";
import * as Stdlib_Array from "@dsiu/rescript-stdlib-fp/src/Stdlib_Array.mjs";

function powersetListMap_(set) {
  if (!set) {
    return {
            hd: /* [] */0,
            tl: /* [] */0
          };
  }
  var x = set.hd;
  var tail_powersets = powersetListMap_(set.tl);
  var with_x = Stdlib_List.map(tail_powersets, (function (it) {
          return {
                  hd: x,
                  tl: it
                };
        }));
  return Stdlib_List.concat(tail_powersets, with_x);
}

function powersetListFlatMap_(set) {
  if (!set) {
    return {
            hd: /* [] */0,
            tl: /* [] */0
          };
  }
  var x = set.hd;
  var tail_powersets = powersetListFlatMap_(set.tl);
  return Stdlib_List.flatMap(tail_powersets, (function (it) {
                return {
                        hd: it,
                        tl: {
                          hd: Stdlib_List.concat({
                                hd: x,
                                tl: /* [] */0
                              }, it),
                          tl: /* [] */0
                        }
                      };
              }));
}

function powersetArrayWithList_(xs) {
  return Stdlib_List.toArray(Stdlib_List.map(powersetListMap_(Stdlib_List.fromArray(xs)), Stdlib_List.toArray));
}

function powersetArrayMap_(set) {
  var match = set.length;
  if (match === 0) {
    return [[]];
  }
  var x = set[0];
  var xs = set.slice(1);
  var tail_powersets = powersetArrayMap_(xs);
  var with_x = tail_powersets.map(function (it) {
        return [x].concat(it);
      });
  return tail_powersets.concat(with_x);
}

function powersetArrayFlatMap_(set) {
  var match = set.length;
  if (match === 0) {
    return [[]];
  }
  var x = set[0];
  var xs = set.slice(1);
  var tail_powersets = powersetArrayFlatMap_(xs);
  return Stdlib_Array.flatMap(tail_powersets, (function (it) {
                return [
                        it,
                        [x].concat(it)
                      ];
              }));
}

var List;

var powersetList = powersetListFlatMap_;

var $$Array;

var flatMap = Stdlib_Array.flatMap;

var powersetArray = powersetArrayFlatMap_;

export {
  List ,
  powersetListMap_ ,
  powersetListFlatMap_ ,
  powersetList ,
  powersetArrayWithList_ ,
  $$Array ,
  powersetArrayMap_ ,
  flatMap ,
  powersetArrayFlatMap_ ,
  powersetArray ,
}
/* No side effect */
