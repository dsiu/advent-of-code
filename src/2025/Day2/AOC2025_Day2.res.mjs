// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib__Int from "@dsiu/rescript-stdlib-fp/src/Stdlib__Int.res.mjs";
import * as Stdlib_BigInt from "@rescript/runtime/lib/es6/Stdlib_BigInt.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib__Tuple2 from "@dsiu/rescript-stdlib-fp/src/Stdlib__Tuple2.res.mjs";
import * as Utils$AdventOfCode from "../../Utils.res.mjs";

function log(prim) {
  console.log(prim);
}

function log2(prim0, prim1) {
  console.log(prim0, prim1);
}

function bigIntRange(start, end_) {
  let _current = start;
  let _acc = [];
  while (true) {
    let acc = _acc;
    let current = _current;
    if (current > end_) {
      acc.reverse();
      return acc;
    }
    _acc = [current].concat(acc);
    _current = current + 1n;
    continue;
  };
}

function parse(data) {
  return Utils$AdventOfCode.splitNewline(data).map(prim => prim.trim()).join("").split(",").map(s => Stdlib_Option.getExn(Stdlib__Tuple2.fromArray(s.split("-").map(s => Stdlib_Option.getExn(Stdlib_BigInt.fromString(s), undefined))), undefined));
}

function isMirror(s) {
  let len = s.length;
  let half = len / 2 | 0;
  let firstHalf = s.slice(0, half);
  let secondHalf = s.slice(half, s.length);
  return firstHalf === secondHalf;
}

function isInvalidId(id) {
  let idStr = id.toString();
  if (Stdlib__Int.isEven(idStr.length)) {
    return isMirror(idStr);
  } else {
    return false;
  }
}

function solvePart1(data) {
  let ret = parse(data).map(param => {
    bigIntRange(param[0], param[1]);
    return [
      1n,
      2n
    ];
  });
  return Utils$AdventOfCode.sumBigIntArray(ret.map(Utils$AdventOfCode.sumBigIntArray));
}

function solvePart2(data) {
  return 2;
}

let Tuple2;

export {
  log,
  log2,
  Tuple2,
  bigIntRange,
  parse,
  isMirror,
  isInvalidId,
  solvePart1,
  solvePart2,
}
/* Stdlib__Int Not a pure module */
