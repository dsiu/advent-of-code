// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Int from "@rescript/runtime/lib/es6/Stdlib_Int.js";
import * as Stdlib_Array from "@rescript/runtime/lib/es6/Stdlib_Array.js";
import * as Primitive_int from "@rescript/runtime/lib/es6/Primitive_int.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Utils$AdventOfCode from "../../Utils.res.mjs";

function log(prim) {
  console.log(prim);
}

function log2(prim0, prim1) {
  console.log(prim0, prim1);
}

function parse(data) {
  return Utils$AdventOfCode.splitNewline(data).map(prim => prim.trim()).map(line => {
    let chars = Array.from(line);
    return [
      Stdlib_Option.getOrThrow(chars[0], undefined),
      Stdlib_Option.getOrThrow(Stdlib_Int.fromString(chars.slice(1).join(""), undefined), undefined)
    ];
  });
}

function math_mod(n, m) {
  return Primitive_int.mod_(Primitive_int.mod_(n, m) + m | 0, m);
}

function calculateNewPosition(current, dir, steps) {
  switch (dir) {
    case "L" :
      return current - steps | 0;
    case "R" :
      return current + steps | 0;
    default:
      throw {
        RE_EXN_ID: "Failure",
        _1: "Invalid direction",
        Error: new Error()
      };
  }
}

function normalizePosition(pos) {
  let normalized = math_mod(pos, 100);
  if (normalized < 0) {
    return normalized + 100 | 0;
  } else {
    return normalized;
  }
}

function calculateRotations(nextPosition, currentPosition) {
  let rotations = Math.abs(nextPosition / 100 | 0);
  let correction = nextPosition <= 0 ? 1 : 0;
  let adjustedCorrection = nextPosition < 0 && currentPosition === 0 ? correction - 1 | 0 : correction;
  return rotations + adjustedCorrection | 0;
}

function solvePart2(data) {
  return Stdlib_Array.reduce(parse(data), [
      50,
      0
    ], (param, param$1) => {
      let currentPosition = param[0];
      let nextPosition = calculateNewPosition(currentPosition, param$1[0], param$1[1]);
      let rotations = calculateRotations(nextPosition, currentPosition);
      let normalizedPosition = math_mod(nextPosition, 100);
      return [
        normalizedPosition,
        param[1] + rotations | 0
      ];
    })[1];
}

function solvePart1(data) {
  return Stdlib_Array.reduce(parse(data), [
      50,
      []
    ], (param, param$1) => {
      let result = param[1];
      let nextPosition = calculateNewPosition(param[0], param$1[0], param$1[1]);
      let normalizedPosition = normalizePosition(nextPosition);
      result.push(normalizedPosition);
      return [
        normalizedPosition,
        result
      ];
    })[1].filter(x => x === 0).length;
}

let startPosition = 50;

let trackLength = 100;

export {
  log,
  log2,
  startPosition,
  trackLength,
  parse,
  math_mod,
  calculateNewPosition,
  normalizePosition,
  calculateRotations,
  solvePart2,
  solvePart1,
}
/* Utils-AdventOfCode Not a pure module */
