// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Id from "rescript/lib/es6/Belt_Id.js";
import * as Stdlib_Option from "rescript/lib/es6/Stdlib_Option.js";
import * as Primitive_option from "rescript/lib/es6/Primitive_option.js";
import * as Primitive_string from "rescript/lib/es6/Primitive_string.js";

function Make(BASE) {
  let make = prim => new Map();
  let containerMake = BASE.containerMake;
  let containerAdd = BASE.containerAdd;
  let containerRemove = BASE.containerRemove;
  let containerHas = BASE.containerHas;
  let containerToArray = BASE.containerToArray;
  let vertexToString = BASE.vertexToString;
  let addVertex = (t, x) => {
    let match = t.get(x);
    if (match !== undefined) {
      return;
    } else {
      t.set(x, containerMake());
      return;
    }
  };
  let removeVertex = (t, x) => t.delete(x);
  let getVertex = (t, x) => {
    let v = t.get(x);
    if (v !== undefined) {
      return containerToArray(Primitive_option.valFromOption(v));
    }
    throw {
      RE_EXN_ID: "Not_found",
      Error: new Error()
    };
  };
  let addEdge = (t, x, e) => {
    addVertex(t, x);
    let c = t.get(x);
    if (c !== undefined) {
      return containerAdd(Primitive_option.valFromOption(c), e);
    }
    throw {
      RE_EXN_ID: "Not_found",
      Error: new Error()
    };
  };
  let removeEdge = (t, x, y) => {
    let v = t.get(x);
    if (v !== undefined) {
      return containerRemove(Primitive_option.valFromOption(v), y);
    } else {
      return false;
    }
  };
  let adjacent = (t, x, y) => {
    let v = t.get(x);
    if (v !== undefined) {
      return containerHas(Primitive_option.valFromOption(v), y);
    } else {
      return false;
    }
  };
  let neighbors = (t, x) => containerToArray(Stdlib_Option.getOr(t.get(x), containerMake()));
  let toString = t => {
    let str = {
      contents: ""
    };
    t.forEach((v, k) => {
      str.contents = str.contents + vertexToString(k) + ": [ " + BASE.containerToArray(v).toString() + " ]\n";
    });
    return str.contents;
  };
  return {
    make: make,
    addVertex: addVertex,
    removeVertex: removeVertex,
    getVertex: getVertex,
    addEdge: addEdge,
    removeEdge: removeEdge,
    adjacent: adjacent,
    neighbors: neighbors,
    toString: toString
  };
}

function make(prim) {
  return new Map();
}

function containerMake(prim) {
  return new Set();
}

function containerAdd(prim0, prim1) {
  prim0.add(prim1);
}

function containerRemove(prim0, prim1) {
  return prim0.delete(prim1);
}

function containerHas(prim0, prim1) {
  return prim0.has(prim1);
}

function containerToArray(prim) {
  return Array.from(prim);
}

function containerToString(s) {
  return Array.from(s).toString();
}

function vertexToString(v) {
  return v;
}

function vertexFromString(v) {
  return v;
}

let BASE_Impl = {
  make: make,
  containerMake: containerMake,
  containerAdd: containerAdd,
  containerRemove: containerRemove,
  containerHas: containerHas,
  containerToArray: containerToArray,
  containerToString: containerToString,
  vertexToString: vertexToString,
  vertexFromString: vertexFromString
};

function make$1(prim) {
  return new Map();
}

function addVertex(t, x) {
  let match = t.get(x);
  if (match !== undefined) {
    return;
  } else {
    t.set(x, new Set());
    return;
  }
}

function removeVertex(t, x) {
  return t.delete(x);
}

function getVertex(t, x) {
  let v = t.get(x);
  if (v !== undefined) {
    return Array.from(Primitive_option.valFromOption(v));
  }
  throw {
    RE_EXN_ID: "Not_found",
    Error: new Error()
  };
}

function addEdge(t, x, e) {
  addVertex(t, x);
  let c = t.get(x);
  if (c !== undefined) {
    Primitive_option.valFromOption(c).add(e);
    return;
  }
  throw {
    RE_EXN_ID: "Not_found",
    Error: new Error()
  };
}

function removeEdge(t, x, y) {
  let v = t.get(x);
  if (v !== undefined) {
    return Primitive_option.valFromOption(v).delete(y);
  } else {
    return false;
  }
}

function adjacent(t, x, y) {
  let v = t.get(x);
  if (v !== undefined) {
    return Primitive_option.valFromOption(v).has(y);
  } else {
    return false;
  }
}

function neighbors(t, x) {
  return Array.from(Stdlib_Option.getOr(t.get(x), new Set()));
}

function toString(t) {
  let str = {
    contents: ""
  };
  t.forEach((v, k) => {
    str.contents = str.contents + k + ": [ " + Array.from(v).toString() + " ]\n";
  });
  return str.contents;
}

let $$String = {
  make: make$1,
  addVertex: addVertex,
  removeVertex: removeVertex,
  getVertex: getVertex,
  addEdge: addEdge,
  removeEdge: removeEdge,
  adjacent: adjacent,
  neighbors: neighbors,
  toString: toString
};

function eq(param, param$1) {
  return param[0] === param$1[0];
}

function cmp(param, param$1) {
  return Primitive_string.compare(param[0], param$1[0]);
}

function hash(param) {
  return caml_hash_final_mix(caml_hash_mix_string(0, param[0]));
}

let T = {
  eq: eq,
  cmp: cmp,
  hash: hash
};

let MutableSetTuple = Belt_Id.MakeComparable({
  cmp: cmp
});

let TupleImpl = {
  T: T,
  MutableSetTuple: MutableSetTuple
};

function make$2(prim) {
  return new Map();
}

function addVertex$1(t, x) {
  let match = t.get(x);
  if (match !== undefined) {
    return;
  } else {
    t.set(x, new Set());
    return;
  }
}

function removeVertex$1(t, x) {
  return t.delete(x);
}

function getVertex$1(t, x) {
  let v = t.get(x);
  if (v !== undefined) {
    return Array.from(Primitive_option.valFromOption(v));
  }
  throw {
    RE_EXN_ID: "Not_found",
    Error: new Error()
  };
}

function addEdge$1(t, x, e) {
  addVertex$1(t, x);
  let c = t.get(x);
  if (c !== undefined) {
    Primitive_option.valFromOption(c).add(e);
    return;
  }
  throw {
    RE_EXN_ID: "Not_found",
    Error: new Error()
  };
}

function removeEdge$1(t, x, y) {
  let v = t.get(x);
  if (v !== undefined) {
    return Primitive_option.valFromOption(v).delete(y);
  } else {
    return false;
  }
}

function adjacent$1(t, x, y) {
  let v = t.get(x);
  if (v !== undefined) {
    return Primitive_option.valFromOption(v).has(y);
  } else {
    return false;
  }
}

function neighbors$1(t, x) {
  return Array.from(Stdlib_Option.getOr(t.get(x), new Set()));
}

function toString$1(t) {
  let str = {
    contents: ""
  };
  t.forEach((v, k) => {
    str.contents = str.contents + k + ": [ " + Array.from(v).toString() + " ]\n";
  });
  return str.contents;
}

let Tuple_Old = {
  make: make$2,
  addVertex: addVertex$1,
  removeVertex: removeVertex$1,
  getVertex: getVertex$1,
  addEdge: addEdge$1,
  removeEdge: removeEdge$1,
  adjacent: adjacent$1,
  neighbors: neighbors$1,
  toString: toString$1
};

function make$3(prim) {
  return new Map();
}

function addVertex$2(t, x) {
  let match = t.get(x);
  if (match !== undefined) {
    return;
  } else {
    t.set(x, new Set());
    return;
  }
}

function removeVertex$2(t, x) {
  return t.delete(x);
}

function getVertex$2(t, x) {
  let v = t.get(x);
  if (v !== undefined) {
    return Array.from(Primitive_option.valFromOption(v));
  }
  throw {
    RE_EXN_ID: "Not_found",
    Error: new Error()
  };
}

function addEdge$2(t, x, e) {
  addVertex$2(t, x);
  let c = t.get(x);
  if (c !== undefined) {
    Primitive_option.valFromOption(c).add(e);
    return;
  }
  throw {
    RE_EXN_ID: "Not_found",
    Error: new Error()
  };
}

function removeEdge$2(t, x, y) {
  let v = t.get(x);
  if (v !== undefined) {
    return Primitive_option.valFromOption(v).delete(y);
  } else {
    return false;
  }
}

function adjacent$2(t, x, y) {
  let v = t.get(x);
  if (v !== undefined) {
    return Primitive_option.valFromOption(v).has(y);
  } else {
    return false;
  }
}

function neighbors$2(t, x) {
  return Array.from(Stdlib_Option.getOr(t.get(x), new Set()));
}

function toString$2(t) {
  let str = {
    contents: ""
  };
  t.forEach((v, k) => {
    str.contents = str.contents + k + ": [ " + Array.from(v).toString() + " ]\n";
  });
  return str.contents;
}

let Tuple = {
  make: make$3,
  addVertex: addVertex$2,
  removeVertex: removeVertex$2,
  getVertex: getVertex$2,
  addEdge: addEdge$2,
  removeEdge: removeEdge$2,
  adjacent: adjacent$2,
  neighbors: neighbors$2,
  toString: toString$2
};

export {
  Make,
  BASE_Impl,
  $$String,
  TupleImpl,
  Tuple_Old,
  Tuple,
}
/* MutableSetTuple Not a pure module */
